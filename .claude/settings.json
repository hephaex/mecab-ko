{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "FILE_PATH=$(echo \"$TOOL_INPUT\" | jq -r '.file_path // empty' 2>/dev/null); if [[ \"$FILE_PATH\" == *.rs ]]; then cd /home/mare/mecab-ko/rust && cargo fmt -- \"$FILE_PATH\" 2>/dev/null; fi; exit 0"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "FILE_PATH=$(echo \"$TOOL_INPUT\" | jq -r '.file_path // empty' 2>/dev/null); for pattern in '.env' 'credentials' 'secret' 'token'; do if echo \"$FILE_PATH\" | grep -qi \"$pattern\"; then echo \"Blocked: potential secret file $FILE_PATH\" >&2; exit 2; fi; done; exit 0"
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Bash(cd /home/mare/mecab-ko/rust && cargo *)",
      "Bash(gh issue *)",
      "Bash(gh pr *)",
      "Bash(gh label *)",
      "Bash(git status*)",
      "Bash(git log*)",
      "Bash(git diff*)"
    ]
  }
}
