[package]
name = "mecab-ko-wasm"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
documentation = "https://docs.rs/mecab-ko-wasm"
readme = "README.md"
description = "WebAssembly bindings for MeCab-Ko (Korean morphological analyzer)"
keywords = ["korean", "morphology", "wasm", "nlp", "mecab"]
categories = ["text-processing", "science", "wasm"]
publish = false

[lib]
crate-type = ["cdylib", "rlib"]
doc = false  # Skip docs for WASM bindings (FFI crate, not meant for Rust docs)

[dependencies]
mecab-ko-core = { version = "0.1.1", path = "../mecab-ko-core", default-features = false }
wasm-bindgen = { workspace = true }
js-sys = { workspace = true }
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }

# Optional: For better error handling in WASM
console_error_panic_hook = { version = "0.1", optional = true }
# Note: wee_alloc removed due to unmaintained status and memory leaks (RUSTSEC-2022-0054)
# Modern WASM targets work well with the default Rust allocator

[dev-dependencies]
wasm-bindgen-test = "0.3"

[features]
default = ["console_error_panic_hook"]

# WASM bindings에서는 일부 lint 완화
[lints.rust]
unsafe_code = "deny"
missing_docs = "warn"

[lints.clippy]
all = "warn"
pedantic = "warn"
nursery = "warn"
unwrap_used = "allow"
expect_used = "allow"
panic = "allow"
